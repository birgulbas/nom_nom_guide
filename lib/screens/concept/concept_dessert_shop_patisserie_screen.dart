import 'package:flutter/material.dart';

class ConceptDessertShopPatisserieScreen extends StatelessWidget {
  const ConceptDessertShopPatisserieScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Dessert Shop / Patisserie")),
      body: Center(
        child: Text("Dessert Shop / Patisserie burada listelenecek."),
      ),
    );
  }
}
