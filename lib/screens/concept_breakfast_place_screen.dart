import 'package:flutter/material.dart';

class ConceptBreakfastPlaceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Breakfast Place")),
      body: Center(
        child: Text("Breakfast Place  burada listelenecek."),
      ),
    );
  }
}
