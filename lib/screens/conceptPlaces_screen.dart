import 'package:flutter/material.dart';

class ConceptScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Concept Places")),
      body: Center(
        child: Text("Konsept kafeler burada listelenecek."),
      ),
    );
  }
}
